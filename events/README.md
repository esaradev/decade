# Event files

JSONL files with one historical event per line.

Format: `{"date": "YYYY-MM-DD", "title": "...", "summary": "...", "impact": "..."}`

File naming: `{era}_{region}.jsonl` (e.g. `1960s_america.jsonl`).
