# Persona cards

Each markdown file in this directory defines one persona. Use YAML frontmatter for structured fields and prose body for backstory.

Required fields: name, age, location, occupation, era, worldview, voice_notes.
Optional: reading_list, any other context.

File naming: `{era}_{descriptor}.md` (e.g. `1960s_factory_worker.md`).
